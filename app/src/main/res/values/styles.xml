<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:listDivider">@drawable/list_divider</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:dropDownListViewStyle">@style/mySpinnerStyle</item>
    </style>

    <!-- elevations -->
    <item name="elev_action_bar" type="dimen">2dp</item>
    <item name="elev_normal" type="dimen">1dp</item>

    <!-- colors -->
    <item name="min_title_width" type="dimen">100dp</item>
    <item name="title_text_size" type="dimen">20sp</item>
    <style name="mySpinnerStyle" parent="android:Widget.ListView.DropDown">
        <item name="android:divider">@android:color/darker_gray</item>
        <item name="android:dividerHeight">1dp</item>
    </style>
</resources>
